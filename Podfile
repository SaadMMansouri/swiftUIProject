# Global platform of the project
platform :ios, '11.0'

target 'SwiftUIInstagram' do
  use_frameworks!

  # Pods for SwiftUIInstagram
  
  pod 'Firebase/Analytics'
  pod 'Firebase/Firestore'

  target 'SwiftUIInstagramTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SwiftUIInstagramUITests' do
    # Pods for testing
  end

end
